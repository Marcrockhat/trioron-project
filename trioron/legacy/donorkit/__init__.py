"""Donor / bench support modules the legacy v1.1 API + CLI depend on.

Relocated from the top-level ``experiments/`` directory so the published
``trioron`` package is self-contained (it no longer imports from a research
folder). These back ``trioron train`` / ``absorb`` and several
``trioron.legacy.api`` functions; they are package-internal, not a public API.
"""
